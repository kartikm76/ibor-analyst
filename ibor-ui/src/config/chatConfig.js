/**
 * Chat UI configuration.
 * Centralises all tunable constants so nothing domain-specific
 * is buried inside component logic.
 */

/** Minimum word count for a valid question (client-side pre-check). */
export const MIN_VALID_WORDS = 2

/** Maximum lines shown in a collapsed AI response bubble. */
export const MAX_LINES_COLLAPSED = 30
